import { Audio } from "react-loading-icons";

export default function Loading() {
  return (
    <div className="fixed left-0 top-0 z-50 flex h-full w-full items-center justify-center">
      <span className="loaderTxt">L &nbsp; ading</span>
    </div>
  );
}
